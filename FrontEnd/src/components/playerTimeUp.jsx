import React, { useState } from 'react';
import { socket } from '../socket';

const PlayerTimeUp = () => {
    
  return (
    <div>
        <h2>Player Time Up</h2>
    </div>
  );
};

export default PlayerTimeUp;