import React, { useState } from 'react';
import { socket } from '../socket';

const HostTimeUp= () => {
    
  return (
    <div>
        <h2>Host Time Up</h2>
    </div>
  );
};

export default HostTimeUp;